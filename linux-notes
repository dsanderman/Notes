#Start notes about linux


sudo apt update
    # apt is more modern than apt-get
    # update downloads pkg lists but doesn't install them. Can be used with -y suffix
sudo apt dist-upgrade
    # Best not to add the "-y" suffix as this command adds and deletes files
    # Can combind them with " && " inbetween them on the same line
    
# How to upgrade vscode when you have downloaded the .deb file from the website:
sudo apt update && sudo apt install code

ssh-keygen -t ed25519 -C "david.sanderman@gmail.com"
    # Generated SSH key pair on my chromebook. Pvt/Pub found in /.ssh dir
    # Files are id_ed25519 and id_ed25519.pub 
sudo pip install pygame-ce
sudo apt install python3-venv
cd  ~
mkdir pygame
cd pygame
python3 -m venv pygame

source pygame/bin/activate
    # To activate the virtual environment. To deactivate, just type "deactivate" in the terminal

pip install pygame-ce
pip list
import pygame
python3 -m pygame.examples.aliens
python3 -m pygame.examples.stars
python3 -m pygame.examples.chimp

pip uninstall pygame-ce

rmdir -rf
    # Deletes a directory and all of its contents. Use with caution!

ls -a
    # Lists all files in the current directory, including hidden files (those that start with a dot)

